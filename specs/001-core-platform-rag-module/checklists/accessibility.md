# Accessibility & Keyboard Operability Checklist: Core Extensible Platform + RAG Concept Module

**Purpose**: Requirements-quality gate over the accessibility/keyboard-
operability requirements specifically (FR-011, FR-012, SC-005, and the
new controls added by FR-013/FR-014) -- author self-review before the
`check:a11y` Playwright + axe-core specs (tasks.md T034/T043) are
implemented against these requirements. Tests whether the requirements
are complete, clear, consistent, and measurable; does NOT test whether
the built UI is actually accessible.
**Created**: 2026-08-04
**Feature**: [spec.md](../spec.md)
**Depth**: Standard | **Audience**: Author, pre-implementation self-review

**Note**: This checklist is generated by the `/speckit-checklist`
command based on feature context and requirements.

## Requirement Completeness

- [x] CHK001 Are accessible-name and keyboard-activation requirements stated for the FR-014 chunking-strategy toggle specifically, or only inferred from FR-011's generic "slider, tab, stepper step, button" list which predates this control? [Completeness, Gap, Spec §FR-011, §FR-014] -- Resolved 2026-08-04: FR-014 now explicitly requires the toggle to satisfy FR-011, and FR-011's canonical control enumeration now names the toggle.
- [x] CHK002 Are requirements defined for how a multi-option toggle (chunking strategy) communicates its currently-selected option to assistive technology, beyond a visual color/style difference? [Completeness, Gap, Spec §FR-014] -- Resolved 2026-08-04: FR-014 now requires a selected-state attribute (`aria-pressed` or equivalent).
- [x] CHK003 Are keyboard-operability requirements stated explicitly for the Compare Variants view's controls (variant-card selection, side-by-side return-to-grid control), or does FR-011's example list (slider/tab/stepper/button) leave card-grid selection uncovered by name? [Completeness, Spec §FR-011, §FR-009] -- Resolved 2026-08-04: FR-011's canonical enumeration now names variant cards and other buttons explicitly, applying identically across both views; FR-009 adds the specific card-grid Tab/Enter-Space rule.
- [x] CHK004 Is a canonical enumeration of "every interactive control" referenced by SC-005 documented anywhere in spec.md, so the automated check's scope is unambiguous rather than left to the implementer's own inventory? [Completeness, Gap, Spec §SC-005] -- Resolved 2026-08-04: FR-011 now enumerates the canonical control set, and SC-005 cross-references it.
- [x] CHK005 Are focus-management requirements defined for what receives focus after the stepper jumps to a non-adjacent step (e.g., top of new step content vs. no change), given FR-003 explicitly allows jumping to any step directly? [Completeness, Gap, Spec §FR-003, §FR-011] -- Resolved 2026-08-04: new Edge Case requires focus to move to the newly active step's first interactive control.
- [x] CHK006 Are requirements defined for whether disabled controls (e.g., a Back/Next button at a pipeline boundary) must be removed from the keyboard Tab order, not merely visually dimmed? [Completeness, Gap] -- Resolved 2026-08-04: FR-011 now requires native `disabled` state, not just visual styling.

## Requirement Clarity

- [x] CHK007 Is "logical order" in FR-011's Tab-order requirement defined -- DOM order, visual reading order, or something else -- given the Pipeline Walkthrough's chart-plus-list layout could plausibly diverge between the two? [Clarity, Ambiguity, Spec §FR-011] -- Resolved 2026-08-04: FR-011 now defines "logical order" as DOM order explicitly.
- [x] CHK008 Is "visible focus indicator" in FR-011 given any measurable criteria (minimum outline width, minimum contrast ratio against adjacent colors), or left to implementation judgment? [Clarity, Measurability, Spec §FR-011] -- Resolved 2026-08-04: FR-011 now requires a minimum 2px outline at at least 3:1 contrast.
- [x] CHK009 Does FR-011 specify which arrow-key behavior applies to the Compare Variants card grid (e.g., grid/arrow navigation between cards) the way it already specifies Arrow keys for sliders and the stepper, or is Tab-only navigation assumed there without being stated? [Clarity, Ambiguity, Spec §FR-011] -- Resolved 2026-08-04: FR-009 now states Tab/Enter-Space is sufficient for the card grid; no dedicated arrow-key navigation is required.
- [x] CHK010 Is the Arrow-key increment for sliders required to match each slider's already-specified `step` value (FR-004/FR-006/FR-013), or is that left as an assumed default of the underlying control rather than a stated requirement? [Clarity, Gap, Spec §FR-004, §FR-006, §FR-013] -- Resolved 2026-08-04: FR-011 now requires each slider's Arrow-key increment to match its own stated `step` value.

## Requirement Consistency

- [x] CHK011 Do FR-011's operability requirements and FR-012's reduced-motion requirement stay consistent where they intersect -- specifically, is it clear that collapsing "any animation or transition" to near-zero duration must not also suppress or delay the visible focus indicator FR-011 requires? [Consistency, Spec §FR-011, §FR-012] -- Resolved 2026-08-04: FR-011 now states the focus indicator is exempt from FR-012's collapse since a static outline isn't itself an animation.
- [x] CHK012 Are focus-indicator requirements stated to apply equally to native controls (sliders, buttons) and custom-styled elements (sample-document/sample-query chips, variant cards), or could a reader conclude FR-011 only governs the native-control examples it names? [Consistency, Gap, Spec §FR-011] -- Resolved 2026-08-04: FR-011 now states this applies identically to native and custom-styled elements.
- [x] CHK013 Does SC-005's "100% of interactive controls" scope apply identically to both the Pipeline Walkthrough and Compare Variants views, given the automated-checks-contract already splits verification into two separate spec files per view? [Consistency, Spec §SC-005] -- Resolved 2026-08-04: SC-005 now states this explicitly.

## Acceptance Criteria Quality

- [x] CHK014 Can SC-005's "100% of interactive controls are operable" be verified as pass/fail without additional interpretation, given CHK004's gap in what counts as "a control"? [Measurability, Spec §SC-005] -- Resolved 2026-08-04 (no separate text change needed beyond CHK004): FR-011's new canonical enumeration, cross-referenced by SC-005, closes this dependency.
- [x] CHK015 Is a target WCAG conformance level (e.g., 2.1 AA) named anywhere in spec.md or plan.md, so the axe-core-based portion of SC-005 has an explicit, agreed-upon rule set rather than whatever axe's default happens to check? [Measurability, Gap, Spec §SC-005] -- Resolved 2026-08-04: SC-005 now names WCAG 2.1 AA-aligned rules (axe-core default ruleset).

## Scenario Coverage

- [x] CHK016 Are keyboard-operability requirements defined for the Compare Variants FIFO-replacement scenario (selecting a third variant via keyboard while two are already selected), separately from the general "every control is operable" requirement? [Coverage, Gap, Spec §FR-009, §Edge Cases] -- Resolved 2026-08-04: FR-009 now states FIFO replacement must be reachable/operable via keyboard identically to the first two selections.
- [x] CHK017 Are requirements defined for what a keyboard user encounters in the empty-retrieved-list state (similarity threshold excludes everything) -- is there a focusable element to convey "no results," or is the state silently non-interactive? [Coverage, Gap, Spec §FR-013, §Edge Cases] -- Resolved 2026-08-04: the max-threshold Edge Case now requires the empty-state message to be within a reachable landmark/heading.

## Edge Case Coverage

- [x] CHK018 Is there a requirement addressing whether color alone conveys which chunk a chart point corresponds to, or which chunking strategy is active, given FR-005/FR-014 describe chart highlighting and strategy state without mentioning a non-color-dependent cue? [Edge Case, Gap, Spec §FR-005, §FR-014] -- Resolved 2026-08-04: FR-005 now requires a non-color means of distinguishing highlighted points; FR-014's toggle already carries text labels plus the new `aria-pressed` state.
- [x] CHK019 Are requirements defined for keyboard/focus behavior at the transition moment when switching documents or chunking strategy triggers the query/results/stepper reset (per the Edge Cases reset rules), e.g., where focus lands after the automatic return to the Document step? [Edge Case, Gap, Spec §Edge Cases] -- Resolved 2026-08-04: the document-switch Edge Case now specifies focus lands on the document-selector chip for the newly active document.

## Non-Functional Requirements

- [x] CHK020 Are requirements defined for announcing dynamic content updates (chunk list changing as sliders move, retrieved results updating) to assistive technology, or does FR-011's scope stop at keyboard reachability/activation without addressing live-region behavior for non-keyboard screen-reader use? [Coverage, Gap, Spec §FR-011] -- Resolved 2026-08-04: new Assumption explicitly excludes ARIA live-region announcements from Milestone 1's SC-005 scope, naming it a future enhancement rather than a silent gap.
- [x] CHK021 Are accessible-name requirements specific enough to distinguish a generic name (e.g., "slider") from a descriptive one (e.g., "Chunk size in words") for controls that share a component type but differ in purpose (chunk-size vs. overlap vs. Top-K vs. threshold sliders)? [Clarity, Spec §FR-011] -- Resolved 2026-08-04: FR-011 now requires purpose-specific accessible names with a concrete example.

## Dependencies & Assumptions

- [x] CHK022 Is the assumption that native `<input type="range">` keyboard behavior (Arrow-key stepping) is consistent enough across the evergreen-browser baseline (Assumptions) validated anywhere, or only implicitly relied upon by the automated check? [Assumption, Spec §Assumptions, §FR-011] -- Resolved 2026-08-04: Assumptions now states this reliance explicitly, ruling out a custom-implemented slider alternative.

## Ambiguities & Conflicts

- [x] CHK023 Could "reachability alone (focus without activation) does not satisfy this requirement" (FR-011) be read as requiring every focusable element to also be independently activatable, even non-interactive elements that might incidentally receive focus (e.g., a chart point)? Is the requirement's scope clearly bounded to controls only? [Ambiguity, Spec §FR-011] -- Resolved 2026-08-04: FR-011 now states its scope is bounded to the enumerated control types; incidentally-focusable non-interactive elements are explicitly excluded.

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
